# Rendered by thememanager — do not edit
# Post-processed by apply-theme to strip # from color values

$lock_outer = rgb({{accent}})
$lock_inner = rgb({{bg}})
$lock_font = rgb({{fg}})
$lock_check = rgb({{warning}})
$lock_fail = rgb({{critical}})
