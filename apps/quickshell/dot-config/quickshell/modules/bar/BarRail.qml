// modules/bar/BarRail.qml
// Solid left rail background owned by the bar stack.
pragma ComponentBehavior: Bound

import QtQuick
import services

Rectangle {
    id: root

    color: Colours.palette.m3surface
}
